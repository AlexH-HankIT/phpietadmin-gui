Hohe Priorität
    - LVM
        - VG erstellen

    - Delete
        - Lösche Allow Regeln, wenn Target gelöscht wird

    - Targets adden
            - Volume Groups vom Dropdown Menu ausnehmen

    - MEHR KOMMENTARE

    - Initiatoren allow
        - Bereits verwendetes Targets nicht anzeigen

Niedrige Priorität:
    - LVM überarbeiten
    - Config Menu

    - Allow
        - Target
            - Add
            - Delete

   -doku
        -sudo (Volle pfade + Parameter)
            vgs
            pvs
            lvs
            vgcreate
            lvcreate
            ietadm --op
            lsblk -rn

        rechte ändern
            /etc/iet/ietd.conf
            /etc/iet/targets.allow
            /etc/iet/initiator.allow

            chown root:www-data /etc/iet/*
            chmod 770 /etc/iet/*

        screenshots
