www-data ALL=NOPASSWD: /sbin/vgs, \
                       /sbin/pvs, \
                       /sbin/lvs, \
                       /usr/sbin/ietadm, \
                       /sbin/lvcreate, \
                       /sbin/lvremove, \
                       /sbin/lvextend, \
                       /sbin/lvreduce, \
                       /sbin/lvrename, \
                       /sbin/shutdown, \
                       /usr/sbin/service cron *, \
                       /usr/sbin/service ssh *, \
                       /usr/sbin/service iscsitarget *, \
                       /usr/sbin/service rsyslog *, \
                       /usr/sbin/service apache2 *

